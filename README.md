# SynthHub

[![CI](https://github.com/tauptlab/synthhub/actions/workflows/ci.yml/badge.svg)](https://github.com/tauptlab/synthhub/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.10%2B-blue)](pyproject.toml)
[![Open in Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/tauptlab/synthhub/blob/main/examples/quickstart.ipynb)

SynthHub -- one unified API for differentially private synthetic data.

```python
from synthhub import Synthesizer

synth = Synthesizer(method="privbayes", epsilon=1.0, random_state=0)
synth.fit(real_df)
synth_df = synth.sample(1000)
report = synth.evaluate(real_df, synth_df, target="label")
```

Use one dataframe API while swapping DP synthesizer backends:

- `privbayes`: DataSynthesizer correlated-mode adapter
- `aim`, `mst`: Private-PGM adapters
- `mwem`, `dpctgan`, `patectgan`, `pategan`, `dpgan`, `quail`: SmartNoise adapters
- `synthcity-privbayes`, `synthcity-pategan`, `synthcity-dpgan`: SynthCity adapters
- `independent`: runnable SynthHub baseline for smoke tests

## Same Dataset, Same Epsilon Benchmark

The benchmark below is generated by `python benchmarks/run_benchmark.py` on a
network-free sklearn dataset. Full outputs live in
[`benchmarks/results/latest.md`](benchmarks/results/latest.md) and
[`benchmarks/results/latest.csv`](benchmarks/results/latest.csv).

![Utility vs risk benchmark](benchmarks/results/utility_vs_risk.svg)

| Method | Backend | Status | Epsilon spent | Utility similarity | TSTR score | Re-ID risk |
|---|---|---|---:|---:|---:|---:|
| `independent` | SynthHub baseline | ok | 1.000 | 0.795 | 0.619 | 0.000 |
| `privbayes` | DataSynthesizer correlated | ok | 1.000 | 0.576 | 0.432 | 0.000 |
| `datasynthesizer-independent` | DataSynthesizer independent | ok | 1.000 | 0.869 | 0.513 | 0.005 |

`Utility similarity` is a distribution-similarity score. `TSTR score` is
train-on-synthetic, test-on-real accuracy. `Re-ID risk` is a membership-inference
heuristic, not a DP proof.

## Install

From GitHub:

```bash
python -m pip install "synthhub[datasynthesizer] @ git+https://github.com/tauptlab/synthhub.git"
```

After the first PyPI release:

```bash
python -m pip install "synthhub[datasynthesizer]"
```

For local development:

```bash
python -m pip install -e ".[test]"
python -m pytest -q
```

For AIM/MST experiments, use Python 3.10-3.12, install the optional Private-PGM
dependency, and make the Private-PGM `mechanisms` folder importable:

```bash
python -m pip install -e ".[private-pgm,test]"
```

On newer Python versions, the core SynthHub package still installs, but the
Private-PGM extra is skipped and the AIM/MST adapters raise a clear
`BackendNotAvailableError` until a compatible external engine is available.

Optional backend families:

```bash
python -m pip install "synthhub[datasynthesizer]"
python -m pip install "synthhub[smartnoise]"
python -m pip install "synthhub[synthcity]"
```

## API

```python
from synthhub import Synthesizer

synth = Synthesizer(method="aim", epsilon=1.0, delta=1e-9)
synth.fit(df)
sample = synth.sample(5000)
report = synth.evaluate(df, sample, target="outcome")

print(synth.privacy_report_.to_dict())
print(report.to_dict())
```

Supported method names:

| Method | Purpose |
|---|---|
| `aim` | Private-PGM AIM adapter; intended default DP marginal backend |
| `mst` | Private-PGM MST adapter |
| `privbayes`, `datasynthesizer-privbayes` | DataSynthesizer correlated-mode PrivBayes adapter |
| `datasynthesizer-independent` | DataSynthesizer independent-attribute DP adapter |
| `independent` | built-in one-way marginal smoke-test baseline |
| `mwem`, `pacsynth`, `dpctgan`, `patectgan`, `pategan`, `dpgan`, `quail` | SmartNoise Synthesizers adapters |
| `smartnoise-aim`, `smartnoise-mst` | SmartNoise AIM/MST adapters |
| `synthcity-privbayes`, `synthcity-pategan`, `synthcity-dpgan` | SynthCity privacy plugin adapters |

## Privacy Notes

SynthHub does not invent new DP algorithms. It wraps existing engines behind a
single dataframe API and records backend-reported privacy accounting.

Formal DP guarantees are conditional on public preprocessing metadata. If
SynthHub infers column types, categories, or numeric bounds from the private
dataframe, the fitted privacy report includes a warning. For formal releases,
pass an explicit public `Schema`.

Read [`docs/dp-guarantees.md`](docs/dp-guarantees.md) for backend-specific
accounting sources, caveats, and CI coverage.

## Development Checks

```bash
python -m pytest -q
python -m pip wheel . --no-deps -w dist
python benchmarks/run_benchmark.py
```

## Community

- Issues and feature requests are welcome.
- Security-sensitive reports should follow [`SECURITY.md`](SECURITY.md).
- Contributions should follow [`CONTRIBUTING.md`](CONTRIBUTING.md).

## GitHub Topics

Add these topics when creating the repository:

- `differential-privacy`
- `synthetic-data`
- `privacy`
